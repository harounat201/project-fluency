import SwiftUI
import UniformTypeIdentifiers
import UIKit

struct ChorusDAFView: View {
    
    var body: some View {
        ZStack {
            
            VStack(spacing: 20) {
             
                }
            }
        }
    }

